---
to: "src/mmdio/engine/render.py"
force: true
---
"""GENERATED FILE — public dispatch for specialized deep-AST renderers.

Regenerate from ``packs/mmdio-pack/templates/generated_render_facade.py.tmpl``.
"""

from __future__ import annotations

from mmdio.engine.models import (
    C4Diagram,
    ClassDiagram,
    ERDiagram,
    FlowchartDiagram,
    GanttChart,
    GitGraph,
    MermaidDiagram,
    Mindmap,
    PieChart,
    SankeyDiagram,
    SequenceDiagram,
    StateDiagram,
)
from mmdio.engine.render_specialized import (
    render_c4,
    render_class,
    render_er,
    render_flowchart,
    render_gantt,
    render_git,
    render_mindmap,
    render_pie,
    render_sankey,
    render_sequence,
    render_state,
)


def render_diagram(diagram: MermaidDiagram) -> str:
    """Render one admitted deep semantic model."""
    dispatch = {
        C4Diagram: render_c4,
        ClassDiagram: render_class,
        ERDiagram: render_er,
        FlowchartDiagram: render_flowchart,
        GanttChart: render_gantt,
        GitGraph: render_git,
        Mindmap: render_mindmap,
        PieChart: render_pie,
        SankeyDiagram: render_sankey,
        SequenceDiagram: render_sequence,
        StateDiagram: render_state,
    }
    try:
        renderer = dispatch[type(diagram)]
    except KeyError as exc:
        raise ValueError(f"Unknown diagram type: {type(diagram)!r}") from exc
    return renderer(diagram)
