PREFIX paas: <https://seanchatmangpt.github.io/ontology/mmdio/paas#>
PREFIX dct: <http://purl.org/dc/terms/>
SELECT ?violation WHERE { FILTER NOT EXISTS { paas:ontology dct:conformsTo <http://www.w3.org/ns/shacl#> . } BIND("ONTOLOGY_SHACL_CONFORMANCE" AS ?violation) }
