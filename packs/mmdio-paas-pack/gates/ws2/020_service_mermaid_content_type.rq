PREFIX paas: <https://seanchatmangpt.github.io/ontology/mmdio/paas#>
SELECT ?violation WHERE { FILTER NOT EXISTS { paas:service paas:contentType "text/vnd.mermaid" . } BIND("SERVICE_MERMAID_CONTENT_TYPE" AS ?violation) }
