PREFIX paas: <https://seanchatmangpt.github.io/ontology/mmdio/paas#>
PREFIX owl: <http://www.w3.org/2002/07/owl#>
SELECT ?violation WHERE { FILTER NOT EXISTS { paas:ontology a owl:Ontology . } BIND("ONTOLOGY_TYPE" AS ?violation) }
