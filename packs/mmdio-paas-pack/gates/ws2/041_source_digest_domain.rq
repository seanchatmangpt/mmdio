PREFIX paas: <https://seanchatmangpt.github.io/ontology/mmdio/paas#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
SELECT ?violation WHERE { FILTER NOT EXISTS { paas:sourceDigest rdfs:domain paas:RenderIntent . } BIND("SOURCE_DIGEST_DOMAIN" AS ?violation) }
