PREFIX paas: <https://seanchatmangpt.github.io/ontology/mmdio/paas#>
PREFIX dct: <http://purl.org/dc/terms/>
SELECT ?violation WHERE { FILTER NOT EXISTS { paas:ontology dct:description ?description . } BIND("ONTOLOGY_DESCRIPTION" AS ?violation) }
